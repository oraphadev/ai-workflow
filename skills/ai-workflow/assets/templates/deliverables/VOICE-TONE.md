# Voice & Tone

> Conductor: fill each section. **Voice** is constant (the brand's personality in words). **Tone** flexes by context.
> Keep this consistent with BRAND.md (personality, values).

## 1. Voice principles

The way the brand always sounds, regardless of context.

1. **`<Principle, e.g. "Clear over clever">`** — `<what it means + a one-line example>`
2. **`<Principle>`** — `<what it means>`
3. **`<Principle>`** — `<what it means>`
4. **`<Principle>`** — `<what it means>`

> Aim for 3–5 principles. Each should be actionable, not vague.

## 2. Tone by context

Same voice, adjusted for the user's emotional state and the moment.

| Context | How the tone shifts | Example copy |
| ------- | ------------------- | ------------ |
| **Error** | `<e.g. calm, blameless, solution-first; never alarming>` | `"<example error message>"` |
| **Success** | `<e.g. warm, brief, celebratory but not over the top>` | `"<example success message>"` |
| **Onboarding** | `<e.g. encouraging, guiding, low-jargon, confidence-building>` | `"<example onboarding line>"` |
| **Marketing** | `<e.g. bold, benefit-led, energetic>` | `"<example headline / CTA>"` |

## 3. Do's and don'ts

### Do

- `<Do this — e.g. "Use second person (\"you\")">`
- `<Do this>`
- `<Do this>`

### Don't

- `<Avoid this — e.g. "Don't blame the user in errors">`
- `<Avoid this>`
- `<Avoid this>`

## 4. Example snippets

Real before/after rewrites help the team internalize the voice.

| Situation | ❌ Off-brand | ✅ On-brand |
| --------- | ----------- | ---------- |
| `<e.g. Empty state>` | `"<flat / generic version>"` | `"<on-brand version>"` |
| `<e.g. Form validation>` | `"<robotic version>"` | `"<on-brand version>"` |
| `<e.g. Confirmation>` | `"<over-the-top version>"` | `"<on-brand version>"` |

## 5. Brand glossary

Preferred terminology — use the right column, avoid the wrong one.

| Term / concept | Preferred usage | Avoid |
| -------------- | --------------- | ----- |
| `<concept>` | `<preferred word>` | `<word(s) not to use>` |
| `<concept>` | `<preferred word>` | `<word(s) not to use>` |
| `<concept>` | `<preferred word>` | `<word(s) not to use>` |

- Capitalization rules: `<e.g. product name always TitleCase>`
- Numbers / units: `<e.g. spell out one–nine, numerals for 10+>`
- Punctuation quirks: `<e.g. no exclamation marks in errors>`

---

_Related deliverables: BRAND.md (positioning & narrative), VISUAL-IDENTITY.md (visual voice)._
