# Tech Stack Decisions

> Records the chosen technology per layer with justification, alternatives, and trade-offs.
> Fill in each `<...>` placeholder. Delete layers that do not apply; add layers as needed.

---

## Frontend

- **Framework:** `<e.g. Next.js 15 / React / SvelteKit>`
- **Styling:** `<e.g. Tailwind CSS / CSS Modules>`
- **UI library:** `<e.g. shadcn/ui / Radix / MUI>`

**Justification:** `<why this fits the project's needs, team skills, constraints>`
**Alternatives considered:** `<option A, option B>`
**Trade-offs:** `<what we give up, e.g. bundle size, learning curve, lock-in>`

---

## Backend

- **Runtime:** `<e.g. Node.js 22 / Bun / Deno>`
- **Framework:** `<e.g. Hono / Express / NestJS>`
- **API style:** `<e.g. REST / tRPC / GraphQL>`

**Justification:** `<...>`
**Alternatives considered:** `<...>`
**Trade-offs:** `<...>`

---

## Data

- **Database:** `<e.g. PostgreSQL / SQLite / MongoDB>`
- **ORM / query layer:** `<e.g. Prisma / Drizzle / raw SQL>`
- **Cache:** `<e.g. Redis / in-memory / none>`

**Justification:** `<...>`
**Alternatives considered:** `<...>`
**Trade-offs:** `<...>`

---

## Auth

- **Solution:** `<e.g. Better Auth / Auth.js / Clerk / custom JWT>`

**Justification:** `<...>`
**Alternatives considered:** `<...>`
**Trade-offs:** `<...>`

---

## Infra / Deploy / Hosting

- **Hosting:** `<e.g. Vercel / Fly.io / AWS>`
- **CI/CD:** `<e.g. GitHub Actions>`
- **Observability:** `<e.g. Sentry / Logflare>`

**Justification:** `<...>`
**Alternatives considered:** `<...>`
**Trade-offs:** `<...>`
