# Brand

> Conductor: fill each section below. Replace all `<placeholders>`. Keep guidance lines or delete them once filled.
> Scope note: This is a **concept-level** brand definition. Domain/trademark/social-handle **availability checks are out of scope** here — flag them as a follow-up if the project needs them.

## 1. Naming

### Chosen name

**`<Name>`**

- Pronunciation: `<how to say it>`
- Type: `<descriptive | invented | metaphor | acronym | compound>`

### Rationale

`<Why this name. 2–4 sentences. Tie it to the product's core value and audience.>`

- Memorable because: `<reason>`
- Fits the audience because: `<reason>`
- Says/implies: `<what it evokes>`

### Alternatives considered

| Name | Why it was tempting | Why it was rejected |
| ---- | ------------------- | ------------------- |
| `<Alt 1>` | `<pro>` | `<con>` |
| `<Alt 2>` | `<pro>` | `<con>` |
| `<Alt 3>` | `<pro>` | `<con>` |

> Availability (domain / trademark / handles) intentionally NOT evaluated here — concept only.

## 2. Positioning

- **Tagline:** `<short, sticky line — ~3–7 words>`
- **One-liner:** `<For <audience>, <Name> is the <category> that <key benefit>, unlike <alternative>.>`
- **Core message:** `<The single idea you want everyone to remember.>`
- **Category:** `<the space you compete in>`
- **Primary audience:** `<who it is for>`
- **Key differentiator:** `<what makes it different, in one phrase>`

## 3. Brand narrative

### The problem

`<The tension or frustration your audience feels today.>`

### The shift

`<What changes / what becomes possible because this exists.>`

### The story

`<2–4 short paragraphs telling the brand story: who it serves, why it exists,
what it believes, where it is going. Write it as you would say it out loud.>`

### Brand values

- `<Value 1>` — `<what it means in practice>`
- `<Value 2>` — `<what it means in practice>`
- `<Value 3>` — `<what it means in practice>`

### Personality

`<3–5 adjectives that describe the brand as if it were a person.>`

---

_Related deliverables: VOICE-TONE.md (how it speaks), VISUAL-IDENTITY.md (how it looks)._
